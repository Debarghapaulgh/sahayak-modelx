# Training-box requirements. On the SageMaker HuggingFace DLC (launch_sagemaker_training.py) torch/transformers/
# accelerate/datasets are preinstalled and these lines are satisfied already; on a bare CUDA box they install.
# Torch must be the CUDA build for your driver (a DL AMI ships it).
# Lessons from 17 failed jobs on 2026-09-19 (each ~440 s of GPU): the sarvam_moe remote code needs
# transformers >= 4.49 (dynamic_rope_update); transformers >= 4.50 needs accelerate >= 1.3
# (unwrap_model keep_torch_compile); trl < 0.15 has no processing_class; the model rejects token_type_ids.
torch>=2.3            # CUDA build (cu121/cu124)
transformers>=4.49,<4.56
peft>=0.14,<0.16
trl>=0.15,<0.20       # 0.20 renamed max_seq_length -> max_length; train_qlora.py handles both
bitsandbytes>=0.45    # 4-bit QLoRA; needs CUDA
accelerate>=1.3
datasets>=2.20
sentencepiece         # tokenizer
hf_transfer           # fast shard download (HF_HUB_ENABLE_HF_TRANSFER=1)
# merge/quant step (Phase F): install ONE of these on the box you quantize on:
#   autoawq            # if quantizing to AWQ
#   llmcompressor      # if quantizing to compressed-tensors (matches the base's format)
