# Install on the GPU training box (CUDA). Torch must be the CUDA build for your driver.
# On an AWS Deep Learning AMI, torch is preinstalled — pip install the rest.
torch>=2.3            # CUDA build (cu121/cu124). DL AMI ships this.
transformers>=4.44
peft>=0.12
trl>=0.9
bitsandbytes>=0.43    # 4-bit QLoRA; needs CUDA
accelerate>=0.33
datasets>=2.20
sentencepiece         # tokenizer
# merge/quant step (Phase F): install ONE of these on the box you quantize on:
#   autoawq            # if quantizing to AWQ
#   llmcompressor      # if quantizing to compressed-tensors (matches the base's format)
